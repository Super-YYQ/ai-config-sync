# /config-sync:capture

Propose and optionally write unmanaged local resources into the private config repository.

```bash
ai-config-sync capture
ai-config-sync capture --yes
ai-config-sync capture --yes --commit
```
