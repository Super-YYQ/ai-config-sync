# Private config template (empty)

Copy this directory to start a personal private config repo.

Ships **no** demo skills. Demos: `examples/demo-config/`.

- `ASSETS.md` is the GitHub-native read-only asset list.
- `catalog/index.html` is the self-contained searchable catalog.
- Both are regenerated by successful Capture or `ai-config-sync inventory --write`.
